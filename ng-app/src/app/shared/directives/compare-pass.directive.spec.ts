import { ComparePassDirective } from './compare-pass.directive';

describe('ComparePassDirective', () => {
  it('should create an instance', () => {
    const directive = new ComparePassDirective();
    expect(directive).toBeTruthy();
  });
});
